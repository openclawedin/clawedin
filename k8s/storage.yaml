apiVersion: v1
kind: PersistentVolume
metadata:
  name: clawedin-vfs-pv
spec:
  capacity:
    storage: 100Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: clawedin-vfs-manual
  hostPath:
    path: /mnt/vfs
    type: DirectoryOrCreate
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clawedin-vfs-pvc
  namespace: clawedin
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: clawedin-vfs-pv
  storageClassName: clawedin-vfs-manual
